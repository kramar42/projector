---
id: pick-a-css-approach
title: Pick a CSS approach
facets:
  status: [planning]
  priority: [now]
  energy: [decide]
  parent: [redesign-the-home-page]
  tech: [css]
  project: [personal-site]
created: 2026-08-26
updated: 2026-08-26
---

The blocker. Mark this `done` and *Unblocked now* gains the note that was waiting on it —
the view is a saved query, not a list anybody maintains.

It also shows what `parent` is for. This note belongs to `personal-site` like its
siblings do, but it is a *piece of* the redesign — so `project` names the project and
`parent` names the note above it. Pointing both at `personal-site` would have stored one
edge twice, and `pj check` says so.
