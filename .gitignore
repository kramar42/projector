node_modules/
.pnpm-store/
work/
*.db
*.db-journal
dist/
.DS_Store
vaults.json

# One lockfile is committed — pnpm's, which is what CI installs from. Every other
# manager works and resolves fresh; their lockfiles just must not land here, or
# two of them drift and nobody knows which one describes the tree.
bun.lock
bun.lockb
package-lock.json
yarn.lock
.impeccable/
